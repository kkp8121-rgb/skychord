{
  "name": "skychord",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SKYCHORD — an original 3D party RPG about restoring a sky railway through harmony",
  "scripts": {
    "build": "node tools/build.mjs",
    "serve": "node tools/server.cjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node tests/browser.cjs",
    "test:all": "npm run build && npm test && npm run test:browser",
    "pack": "node tools/pack.cjs"
  },
  "devDependencies": {
    "three": "0.186.0",
    "esbuild": "0.28.2",
    "playwright": "1.63.0"
  }
}
